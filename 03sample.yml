- name: Change the file
  hosts: localhost
  connection: local
  tasks:
    - name: copy the file
      ansible.builtin.uri:
        url: http://www.google.com
        dest: /etc/dest
      register: out
    - name: check the url is reached
      ansible.builtin.debug:
        var: out